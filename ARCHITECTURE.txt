DUAL-INDEX ARCHITECTURE EXPLAINED
===================================

PHASE 1: BUILD PERSISTENT KB (Parallel)
----------------------------------------
documents/
├── doc1.pdf ────┐
├── doc2.pdf ────┼──> [Parallel Indexing] ──> documents-index (PERSISTENT)
└── doc3.pdf ────┘                              ├── doc1 chunks (permanent)
                                                 ├── doc2 chunks (permanent)
                                                 └── doc3 chunks (permanent)

Result: Permanent searchable knowledge base

PHASE 2: EPHEMERAL Q&A (Sequential, Isolated)
----------------------------------------------
For doc1.pdf:
  1. Index to temp-documents-index (session_id: ephemeral_abc123)
     └── Only doc1 chunks present (ISOLATED)

  2. Answer questions:
     Q1: "What risks?" ──> Vector Search ──> Only doc1 chunks ──> GPT ──> Answer
     Q2: "Adaptation?"  ──> Vector Search ──> Only doc1 chunks ──> GPT ──> Answer

  3. Delete session_id: ephemeral_abc123
     └── temp-documents-index now EMPTY

For doc2.pdf:
  1. Index to temp-documents-index (session_id: ephemeral_xyz789)
     └── Only doc2 chunks present (ISOLATED)

  2. Answer questions (no interference from doc1)

  3. Delete session_id: ephemeral_xyz789

...and so on for each document

FINAL STATE:
------------
✓ documents-index: All documents (permanent)
✓ temp-documents-index: Empty (all cleaned up)
✓ output/qa_results.csv: All answers (accurate)

BENEFITS:
---------
1. Maximum Accuracy: Each Q&A session sees only ONE document
2. No Cross-Document Noise: Vector search can't retrieve wrong chunks
3. Permanent KB: Can query later without re-indexing
4. Minimal Temp Storage: Only one doc in temp at a time
5. Parallel KB Building: Fast initial indexing
